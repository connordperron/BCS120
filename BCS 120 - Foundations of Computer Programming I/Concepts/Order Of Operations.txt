#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main()
{
	double a, b, c, d, e, f, g;

	a = 2 * 2;
	b = pow(2, 2);
	c = 2 ^ 2;
	d = 2 + 2 - 2 * 2;
	e = 2 + (2 - 2) * 2;
	f = (2 + 2 - 2) * 2;

	cout << a << "\t" << b << "\t" << c << "\t" << d << "\t" << e << "\t" << f << endl;

}